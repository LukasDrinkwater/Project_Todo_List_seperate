const List = (name, existingTasksArray = []) => {
  const tasksArray = existingTasksArray || [];
};

const printTasks = () => {
  tasks.forEach((task) => {
    // eslint-disable-next-line no-console
    console.log(task);
  });
};
