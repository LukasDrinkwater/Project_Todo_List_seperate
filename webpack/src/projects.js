class Projets {
  // task object
  constructor(task, assignToThisProject) {
    this._task = task;
    this._assignToThisProject = assignToThisProject;

    let taskArray = [];
  }
  addTaskToListArray(task) {}
}
